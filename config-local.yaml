# ============================================================
# Agent 本地配置（连接远程 MCP Server）
# 不需要数据库连接配置，数据库连接由远程 MCP Server 管理
# ============================================================

# Agent 配置
agent:
  default_cluster: "test"

  # --- Claude (Anthropic) ---
  # provider: "anthropic"
  # model: "claude-sonnet-4-20250514"
  # max_tokens: 4096
  # api_key: "${ANTHROPIC_API_KEY}"

  # --- DeepSeek ---
  # provider: "openai_compatible"
  # model: "deepseek-chat"
  # max_tokens: 4096
  # api_key: "${DEEPSEEK_API_KEY}"
  # base_url: "https://api.deepseek.com"

  # --- GPT (OpenAI) ---
  # provider: "openai_compatible"
  # model: "gpt-4o"
  # max_tokens: 4096
  # api_key: "${OPENAI_API_KEY}"
  # base_url: "https://api.openai.com/v1"

  # --- GLM (智谱) ---
  provider: "openai_compatible"
  model: "glm-5.1"
  max_tokens: 4096
  api_key: "${LLM_API_KEY}"
  base_url: "${LLM_BASE_URL}"

  # --- Qwen (通义千问) ---
  # provider: "openai_compatible"
  # model: "qwen-plus"
  # max_tokens: 4096
  # api_key: "${DASHSCOPE_API_KEY}"
  # base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"

mcp_servers:
  - name: "mcp-server"
    url: "${MCP_SERVER_URL}"
    api_key: "${MCP_API_KEY}"
